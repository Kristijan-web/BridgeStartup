export interface PostBadgesInterface {
    // Koje kolone ima tabela Badges?
    // - id
    // - badgeId
    // - postId
    // - createdAt
    // - updatedAt
    // - deletedAt

    id: string,
    badgeId: string,
    postId: string,
    createdAt: Date,
    updatedAt: Date,
    deletedAt: Date,
    
}
