import { Component } from '@angular/core';
@Component({
  selector: 'app-hero',
  template: `<section class="mx-auto max-w-6xl px-6 py-12">
    <p class="eyebrow">Startup opportunities</p>
    <h1 class="mt-4 max-w-3xl text-4xl font-black sm:text-5xl">Bridge the gap between founders and builders.</h1>
    <p class="mt-5 max-w-3xl text-lg leading-8 text-slate-600">Discover startup ideas, explore the technology behind them, and connect directly with founders to build something together.</p>
  </section>`
})
export class Hero {}
